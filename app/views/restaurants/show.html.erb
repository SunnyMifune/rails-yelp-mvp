<h2><%= @restaurant.name %></h2>
<p><%= @restaurant.address %></p>
<p><%= @restaurant.phone_number %></p>
<p><%= @restaurant.category %></p>

<%# <p><strong>Reviews</strong></p>
<ul class="list-group">
  <% @restaurant.reviews.each do |review| %>
    <%# <li class="list-group-item">
    <%= review.content %>
    <%# <%= review.rating %>
    <%# </li> %>
  <%# <% end %> %>
<%# </ul> %> %>

<%# <%= link_to "home", restaurants_path %> %> %> %>
<p class="fw-lighter opacity-75 mt-2"><%= Faker::Restaurant.description  %></p>
      <h3>Reviews <%= link_to '+', new_restaurant_review_path(@restaurant) %></h3>
      <ul>
        <% if @restaurant.reviews.any? %>
          <% @restaurant.reviews.each do |review| %>
            <li>
              <%= review.content %>
              <%= link_to 'x', review_path(review), data: { turbo_method: 'delete', turbo_confirm: 'Delete Review?' }  %>
            </li>
          <% end %>
        <% else %>
          <li>No reviews yet</li>
        <% end %>
      </ul>
    
